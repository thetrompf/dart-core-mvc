library controller_action_result_test.integration.core_mvc;

import 'package:test/test.dart';

void main() {
  group('Integration', () {
    group('Controller - ActionResult', () {
      test('Controller action returns an action result', () {
        expect(1, 1);
      });
    });
  });
}
